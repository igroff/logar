{
  "name": "application",
  "version": "0.0.1",
  "description": "A web application using express and stuff",
  "main": "server.js",
  "scripts": {
    "start": "export APP_NAME=${APP_NAME-$(basename `pwd`)}; test -r ~/.${APP_NAME}.env && . ~/.${APP_NAME}.env ; node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "connect": "3.1.1",
    "express": "4.8.6",
    "lockfile": "1.0.0",
    "morgan": "1.2.3",
    "simplog": "0.1.0"
  },
  "devDependencies": {
    "http-server": "^0.6.1",
    "supervisor": "^0.6.0"
  }
}
